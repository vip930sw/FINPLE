/*
  Temporary safety disable for MY PAGE Account Status DOM patch.
  The previous version used a document-wide MutationObserver and could cause repeated DOM updates on /mypage.
  This file is intentionally left as a no-op until the Account Status card layout is moved into React/CSS directly.
*/

export {};

# Internal review-only app-preview data is opt-in and disabled by default.
# Point this URL at an operator-controlled local HTTP server that serves the
# extracted finple_app_preview_export_<date> directory.
VITE_FINPLE_APP_PREVIEW_ENABLED=false
VITE_FINPLE_APP_PREVIEW_BASE_URL=http://127.0.0.1:8765
